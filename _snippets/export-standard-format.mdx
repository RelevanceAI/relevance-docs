### Export to CSV
Export to CSV creates a copy of all the columns in your table and exports it as a CSV file.
Select the dataset from [data table](/get-started/key-concepts/data) and click on the `Download as CSV` button.
Export type is set to "Standard" by default.

![export](/images/general/export.png)

Clicking on `Download` will download a CSV file to your download folder.
